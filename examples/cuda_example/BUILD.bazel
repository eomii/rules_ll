load("@rules_ll//ll:defs.bzl", "ll_binary")

ll_binary(
    name = "cuda_example",
    srcs = ["example.cpp"],
    compilation_mode = "cuda_nvptx",
    compile_flags = [
        "--std=c++20",
        "--offload-arch=sm_70",
        "-Wno-deprecated",
    ],
    visibility = ["@//:__pkg__"],
)
